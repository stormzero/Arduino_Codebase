// Simulate(Uno)
// Saved by Simulator for Arduino V0.97.B.2
// Saved by Simulator for Arduino V0.97.B.2
// Saved by Simulator for Arduino V0.97.B.2
// Saved by Simulator for Arduino V0.97.B.2

#include <Servo.h> 
 
Servo myServo,s2,s3,s4,s5,s6,s7,s8,s9,s10,s11,s12;  // create servo object to control a servo 
 

void setup() {
  // put your setup code here, to run once:

 myServo.attach(3);
 s2.attach(4);
 s3.attach(5);
 s4.attach(6);
 s5.attach(7);
 s6.attach(8);
 s7.attach(9);
 s8.attach(10);
 s9.attach(11);
 s10.attach(12);
 s11.attach(13);
 s12.attach(14);
}

void loop() {
  // put your main code here, to run repeatedly:

}
