// Saved by Simulator for Arduino V0.98C refer to arduino.com.au

// Saved by Simulator for Arduino V0.98.B.2
// Saved by Simulator for Arduino V0.98.B.2
// Saved by Simulator for Arduino V0.98.B.2
// This sketch is provided as an instruction on various features of
// the Simulator for Arduino

boolean b1 = FALSE;
char c = 'A';
byte b = 1;
int i = 2;
long l = 3L;
float f = 0.1;
String s = "Hi";


void setup() {
// Simulate(Uno)
 i = 0;
// Resize the sketch window
// by holding right edge and dragging out
// to be wide to see all these comments, especially the line below.
// The cursor will be reset to the line above. Press F7 to step once
 i = 121;
// Now click the value in the middle list to change the value to 100
 if (i==100) {
     pinMode(3,OUTPUT);
 }

/*// The code below shows how the Arduino boards can be changed 
// from within the comments
// Simulate(Uno) */
 i = 1;
// Simulate(Due)
 i = 2;
// Simulate(Mega75)
 i = 3;
// Simulate(Uno)
 
// now right click in the variables window and select watch
// then single step below
 b1 = TRUE;
 c = 'B';
 b = 123;
 i = 256;
 l = 1234567L;
 f = 1.2345;
 s = "Hello There";
// now hover the mouse over each variable

// now click edit and add your oen line of code below



}

void loop() {
  // put your main code here, to run repeatedly:
    i++;
    i++;

}
